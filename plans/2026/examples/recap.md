# Python
- характеристика языка
  - популярный язык
  - области применения: МО
  - интерпр. язык -> медленный
    - использовать скомпил. библиотеки -- так быстрее
- основы синтаксиса
  - нет операторных скобок, есть отступы, : перед влож. блоком
- подключение библиотек, модулей, создание модулей 
- циклы, функции,
- всё - объект
- класс - ссылочный тип
- f строки


# Условный оператор
```py
if условие :
    then_блок
else:
    else_блок
```

```py
if условие1 :
    then_блок1
elif условие2:    
    then_блок2
else:
    else_блок
```


# Циклы
- for
  ```py
  for i in range(начало, конец(не включительно), шаг) :
      тело_цикла
  ```

- while
  ```py
  while условие :
      тело_цикла
  ```


# Функции

```py

def func_name( формальные_параметры ) -> тип_возвр_знач :
    """описание функции"""
    тело_функции
    return ...
```

# Строки
s = "строка"

s = 'строка'

s = """строка 
с
переносами
строк
"""

s = s + "!"

x = 342.4234
s = f"laskdjsldfjsld sldfsdf {x:.2f}"

# Модули
```py
import unit_name

import unit_name as new_unit_name 

import some_item from unit_name



import sqrt from math

from math import *

import math
# вызов функции
math.sqrt( ... )
```


# Типы
```py

x : int = 374
x = 375

```

- Immutable types: int, float, str, tuple
